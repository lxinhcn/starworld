#define asddf( a, b )\
	if( a < b )\
	{\
		return a;\
	}\

#define MMO_LOGIC_MESSAGE_TYPE		0xc0
#define MMO_CONTROL_MESSAGE_TYPE	0xe0

struct item
{
	int oid;
	int sid;
	short posx;
	short posy;
};

message MMO_LogonRequest( MMO_CONTROL_MESSAGE_TYPE, C2S_LogonRequest )
{
	// 掩码
	enum abcd{ a, b, c, d };
	unsigned int mask;
	vector< string > all[9];
	string username;
	string password;
	struct buffer
	{
		char data[1024];
		int	size;
	};
};

message MMO_LogonReplay( MMO_CONTROL_MESSAGE_TYPE, S2C_LogonReplay )
{
	struct item
	{
		int oid; // 物品全局ID
		int sid; // 物品服务器ID
		short posx;
		short posy; // 物品位置
	};
	
	vector< item > items;
};